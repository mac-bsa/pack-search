# Ubuntu configutration

```
curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -
sudo apt-get install -y nodejs
node --version
npm install -g elm
elm --version
npm install -g elm-format
elm-package install fbonetti/elm-geodesy

elm-make Hello.elm --output elm.js
```